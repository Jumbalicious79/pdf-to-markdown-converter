# PDF to Markdown Converter Requirements

# Core - high-quality PDF to markdown (includes pymupdf)
pymupdf4llm>=1.27.0

# Image processing
Pillow>=10.0.0

# Optional: Alternative backend with better table extraction
# pdfplumber>=0.10.0

# Optional: LLM-powered cleanup (install manually if using --cleanup)
# anthropic>=0.40.0
